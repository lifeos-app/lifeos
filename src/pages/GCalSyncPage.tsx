/**
 * GCalSyncPage — Page wrapper for Google Calendar Sync Panel
 */

import { GCalSyncPanel } from '../components/GCalSyncPanel';

export function GCalSync() {
  return <GCalSyncPanel />;
}

export default GCalSync;